import {StyleSheet} from "react-native";

export const styles: any = StyleSheet.create({
    map: {
        width: '100%',
        height: '100%'
    },
    filterView: {
        position: 'absolute',
        top: 10,
        right: 10,
        zIndex: 1000,
        backgroundColor: 'white',
        padding: 10,
    },
})
